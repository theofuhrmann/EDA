#include <iostream>
#include <set>
using namespace std;

int main() {

	int n;
	set<int> s;
	set<int>::iterator it;

	while (cin >> n) {

		if (v[i] >= n) c = 0;
		else if (v[i]%2 == n%2) c = 0;
		else c++;
		i++;
		v.push_back(n);
		cout << s.size() << endl;
	}
}
